
sprint1: 
    creation FrontServlet
    pointage url vers FrontServlet

sprint2: 
    Creation Mapping(String className, String method)
    HashMap<string, Mapping> dans FrontServlet

sprint3:
    Creation annotation 'url'
    Ajout de url dans une methode de classe Emp
    Mameno HashMap tao amle FrontServlet : < j'ai créé une classe Utile pour mettre la fonction pour que le code dans FrontServlet paraisse moin long, et ajouté le nom du package de model dans web.xml

sprint4:
    division en Framework et test-framework
    build.bat

sprint5:
    ModelView
    Changement method avec retour ModelView
    Boucle MappingUrls si == URL sur navigateur dia maka valeur Mapping izay misy nameClass sy nameMethod
    dispatch makany am page hafa avy am ModelView

sprint-6:
    ajout HashMap data dans ModelView
    recuperation de data dans frontServlet avant de dispatcher

sprint-7:
    recuperation formulaire de atao amle classe si attribut==parametre

sprint-8:
    fonction avec parametre


out.println("------------TRAITEMENT FILE------------");
                        try {
                            out.println("------------TRAITEMENT FILE------------");
                            // String methodName = "set" + capitalizeFirstLetter(field.getName());
                            // Method method = object.getClass().getMethod(methodName, FileUpload.class);
                            // FileUpload paramValue = traitementFile(paramName, request);
                            // method.invoke(object, paramValue);
                            
                        } catch (Exception e) {
                            // TODO: handle exception
                            e.printStackTrace(out);
                        }